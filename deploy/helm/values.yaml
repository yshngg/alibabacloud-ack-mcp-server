# Default values for mcp-chart.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

# Required: Alibaba Cloud Access Key ID
accessKeyId: <your-access-key-id>
# Required: Alibaba Cloud Access Key Secret
accessKeySecret: <your-access-key-secret>

transport: sse
port: 8000
allowWrite: true

# Host binding address. Use "127.0.0.1" for localhost-only access (recommended for security).
# Use "0.0.0.0" to expose to all network interfaces (requires proper authentication and Origin validation).
host: "127.0.0.1"

image:
  registry: registry-cn-beijing.ack.aliyuncs.com/acs/ack-mcp-server
  tag: "v1.1.0-4e2343f-aliyun"
  pullPolicy: Always

# Probes (disabled as per requirements)
livenessProbe: { }
readinessProbe: { }

serviceAccount:
  create: true
  annotations: { }

# Replica count for each service
replicaCount: 1
